# Python env (project)
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt   # if the file exists
pip freeze > requirements.txt

# Git identity (ONE TIME per computer)
git config --global user.name  "vije1711"
git config --global user.email "vijendra.singh1711@gmail.com"

# Repo hookup (run once per repo)
git init
# if origin already exists, use: git remote set-url origin https://github.com/vije1711/Birha
git remote add origin https://github.com/vije1711/Birha
git fetch origin
git pull origin main --allow-unrelated-histories

# First commit/push
git add -A
git commit -m "Initial setup: venv requirements + files"
git branch -M main
git push -u origin main

# (optional) Open editor
code .